# Gold Master API Contracts

## Verified Endpoints
- GET /health
- GET /ready
- GET /api/deals
- GET /api/portfolio
- GET /api/operations

## Contract Notes
The current backend contract remains compatible with the validated release baseline. Responses are structured, safe, and resilient to partial or malformed payloads.
