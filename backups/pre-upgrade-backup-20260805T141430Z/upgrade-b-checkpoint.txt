Upgrade B checkpoint
====================

State: Operations, monitoring, and backend health visibility integrated into existing RSOS dashboard surfaces without changing the Command Center layout.

Verified:
- npm test
- npm run build
- live health endpoint: http://127.0.0.1:3001/api/health
- live monitoring endpoint check (expected fallback when operations routes are absent)

Notes:
- The app shell status badge now polls the backend health endpoint and updates live.
- The dashboard now exposes workflow status, recovery counts, alert counts, and monitoring status in the existing intelligence strip area.
- The shared operations integration layer provides safe normalization and summary helpers for future UI expansions.
